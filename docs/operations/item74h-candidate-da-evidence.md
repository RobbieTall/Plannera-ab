# Item 74H candidate Byron shed evidence

Status: PROTECTED PREVIEW CANDIDATE / NOT REVIEWED / NOT PROMOTED

Last updated: 2026-09-01

## Purpose

This slice screens a second public Byron shed case to determine whether its
survey and plans can close the legal-boundary evidence gap that remains in the
accepted Wilsons Creek example. It does not replace, weaken or alter the
existing reviewed outcome.

## Exact public case

The candidate is Byron Shire Council development application
`10.2026.223.1` for 33 Lorikeet Lane, Mullumbimby, Lot 138 DP 1265934.
Council's tracker records a determined and approved shed application dated
14 July 2026.

Only these exact Council records are in scope:

| Role | Council record |
| --- | --- |
| DA stamped plans | `E2026/47502` |
| Shed site plan | `E2026/47506` |
| Survey | `E2026/47509` |
| Determination cross-check | `E2026/80895` |

The exact zone, surveyor registration, legal boundary status and road, side and
rear setbacks remain unconfirmed until a protected operator reviews the CLEAN
pages. Residential property metadata and nearby planning material are not
evidence of this lot's zone.

## Protected execution contract

The acceptance is disabled unless all of these are true:

- `VERCEL_ENV=preview`;
- `VERCEL_GIT_COMMIT_REF` is exactly
  `agent/item74h-candidate-evidence-20260901`;
- `ITEM74H_CANDIDATE_DA_ACCEPTANCE_ENABLED=true`;
- the dedicated private Preview Blob store is available; and
- both checkout flags are absent or false.

The acceptance must:

- parse the live Council tracker and require the exact approved case metadata;
- accept only four exact HTTPS PDF capabilities from the official Byron host;
- enforce 2 MB per-file and 8 MB total limits;
- quarantine each source in private Blob and prove anonymous reads fail;
- refresh ClamAV definitions before creating a deny-all scanner snapshot;
- prove the scanner cannot access the network;
- verify hashes before and after scanning;
- reject malware, encrypted PDFs, limit failures and malformed PDFs;
- retain only page 1 of the survey, site plan and stamped plans as private PNGs;
- delete every source PDF, sandbox and snapshot and prove zero transient
  residue;
- emit no secret, signed capability, content hash or raw document content;
- record no operator decision and promote no evidence;
- write no database row or paid artefact binding; and
- leave Production checkout disabled.

The three private review images are intentional protected operator evidence,
not transient residue. A failed run deletes every candidate page it attempted
to retain.

## Promotion rule

A CLEAN scan proves only that the files can be reviewed safely. It does not
prove their planning meaning.

Promotion requires a human review that confirms, with exact page references:

- the applicable LEP zone from an authoritative spatial source;
- whether the survey identifies a registered surveyor and legal boundaries;
- which depicted dimension is the legal road setback;
- which depicted dimensions are the legal side and rear setbacks; and
- consistency between survey, site plan, stamped plans and determination.

Until then the candidate remains `MORE_EVIDENCE_REQUIRED`, no confirmed fact
is copied into the free Pathway Check, A$49 pack or A$749 SEE, and no
submission-ready claim is permitted.

No Production data, schema, payment or checkout change is authorized.
