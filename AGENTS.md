# Plannera Agent Instructions

All repository work must follow `AGENT_EXECUTION_PROTOCOL.md`.

The repository documentation is the canonical source of product and delivery continuity. Keep `README.md`, `docs/project-memory/`, decision records, and operating records aligned with every material implementation or acceptance change.

Production remains disabled unless the user explicitly approves a separately scoped production action. Never expose secrets or private customer evidence in output, logs, fixtures, artefacts, or documentation.
