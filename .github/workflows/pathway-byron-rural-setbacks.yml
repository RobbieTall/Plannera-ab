name: Byron Rural Setback Contract

on:
  pull_request:
    paths:
      - "src/lib/pathway-byron-rural-setbacks.ts"
      - "src/lib/pathway-byron-rural-setbacks.test.ts"
      - "docs/operations/item74h-byron-rural-setbacks.md"
      - ".github/workflows/pathway-byron-rural-setbacks.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run Byron rural setback contract
        run: npx vitest run src/lib/pathway-byron-rural-setbacks.test.ts
